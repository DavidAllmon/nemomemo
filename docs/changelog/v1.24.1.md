---
version: 1.24.1
date: 2026-08-24
---

## What's new
- Housekeeping under the sea — nothing you'll notice, just a tidier reef. 🫧

## Technical notes
- Test-only. Three more read paths added to the trash guard matrix in
  `server/src/test/trash.test.ts`: the raw file server (a trashed memo's attachment
  404s even for its owner — an attachment can never out-scope its memo), the
  attachment gallery's tag join, and the markdown export (skipped via
  `checkMemoRead`). All three already behaved correctly; they were listed in the plan
  and missed in the first pass, and the matrix is the thing that catches the next
  hand-rolled WHERE. Server suite: 219 → 222.
