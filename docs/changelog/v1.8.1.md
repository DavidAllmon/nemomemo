---
version: 1.8.1
date: 2026-08-23
---

## What's new
- **A proper welcome at the door.** New members now get one friendly welcome
  email when they join — what the reef is about, plus the confirm-your-email
  link, all in a single message instead of two.

## Technical notes
- Signup sends `welcomeMessage` (verify link embedded) via
  `sendVerificationEmail(..., {welcome: true})`; email changes and resends keep
  the plain `verifyEmailMessage` copy. Assertions added to
  email-identity.test.ts (subjects for both paths).
- Ops (no code): Brevo SMTP live in cloud.env (cloud reefs `emailEnabled: true`;
  demo intentionally left mail-less), SMTP smoke-send verified; CLOUD-OPS
  monitoring section now recommends a single Better Stack account (uptime
  monitors + heartbeats) over the two-tool setup.
